services:
  together-frontend:
    build:
      context: .
      args:
<<<<<<< HEAD
        - EXPO_PUBLIC_API_URL=${API_URL:?ERREUR_VARIABLE_API_URL_MANQUANTE_DANS_COOLIFY}
=======
        - EXPO_PUBLIC_API_URL=${API_URL}
>>>>>>> b678e8c (fix: TA-132 modif and add env variable)
    container_name: together-frontend
    restart: unless-stopped
    ports:
      - "3000:80" #Host:Container
    environment:
      - NODE_ENV=production
      - EXPO_PUBLIC_API_URL=${API_URL}
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:80 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s